spring:
  application:
    name: recruitment-service
  profiles:
    active: dev
  cloud:
    openfeign:
      client:
        config:
          recruitment-ai-service:
            connectTimeout: 10000
            readTimeout: 120000

logging:
  level:
    com.recruitment: debug
